import { writeAndRead } from "./writeAndReadObject.js";
import { readConsole } from "./readConsole.js";
import * as fs from "fs";
let id = {
  name: "Pascual",
  surname: "Vera",
  age: "28",
};

readConsole(id);
